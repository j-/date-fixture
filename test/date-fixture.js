const dateFixture = require('../');
const assert = require('assert');

describe('dateFixture', function () {
	it('is a function', function () {
		assert.equal(typeof dateFixture, 'function', 'Check type');
	});
});
